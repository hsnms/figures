%\documentclass{article}
%\usepackage{tikz}
%\usepackage{pgfplots}
%\begin{document}




 \def \rl {2*0.7}
  \def \rh {1*0.7}  
 % \def \rk {2 and 0.58}
  \def \i {2*0.7/5.5}
  \def \a {0.2*0.7}
\def \b {0.21*0.7}
  \def \c {0.08*0.7}
  \def \d {2.001*0.7}
  \begin{tikzpicture}
%\draw (-\rl,\rl) arc (180:0:\rk);
%\draw[dashed] (-\rl,-\rl) arc (180:0:\rk);
 \draw[thick](-\rl,\rl)  -- (-\rl,-\rl);
  \draw[thick](\rl,\rl)   -- (\rl,-\rl);
\draw[thick](-\rl,\rl) -- (\rl,\rl);
  \draw[thick](-\rl,-\rl) -- (\rl,-\rl);
  \draw[thick, ->] (-1.25*\rl,0) -- (1.25*\rl,0) node[right] {$r$};
  \draw[thick,->] (0, -1.25*\rl) -- (0, 1.25*\rl) node[above] {$z$};
         \fill [blue!60!red, opacity=0.2] (0,0) -- (\rl,0) -- (\rl,\rl)--(0,\rl)--cycle;
        \fill [red, opacity=0.2] (0,0,0) -- (\rl,0) -- (\rl,-\rl)--(0,-\rl)--cycle;
           \fill [red, opacity=0.2] (0,0) -- (-\rl,0) -- (-\rl,\rl)--(0,\rl)--cycle;
           \fill [red, opacity=0.2] (0,0) -- (-\rl,0) -- (-\rl,-\rl)--(0,-\rl)--cycle;
  \draw [yellow,opacity=0.5] (\rl,-\rl+\b)--(-\rl,-\rl+\b);
\fill [yellow,opacity=0.2] (\rl,-\rl+\b)--(-\rl,-\rl+\b)--(-\rl,-\rl)--(\rl,-\rl)--cycle; 
\draw [yellow,opacity=0.5] (\rl,\rl-\b)--(-\rl,\rl-\b);
\fill [yellow,opacity=0.2] (\rl,\rl-\b)--(-\rl,\rl-\b)--(-\rl,\rl)--(\rl,\rl)--cycle;   
  

  \foreach \x in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\foreach \y in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw [thick, blue]  (\xx, \yy) -- ++(\zz:\a);
  }
  }

 
 \foreach \x in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\foreach \y in {\rl-5*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{0.5*(\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:\a);
 }
 }   
  
  
   \foreach \x in {-\rl+5*\i} {
\foreach \y in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{2*(\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw[thick, blue] (\xx, \yy) -- ++(\zz:\a);
  }
  }   
  
  \foreach \x in {-\rl+5*\i-0.6*\a} {
  \foreach \y in {\rl-5*\i+0.6*\a} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw[thick,blue] (\xx, \yy) -- ++(\zz:0.9*\a);
 }
 }   
 
 %number 1%
 
 \foreach \x in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\foreach \y in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(-\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw[thick, blue]   (\xx, \yy) -- ++(\zz:\a);
  }
  }   
\foreach \x in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\foreach \y in {\rl-5*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{0.5*(-\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:\a);
 }
 }   
  
  
   \foreach \x in {\rl-5*\i} {
\foreach \y in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{2*(-\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:\a);
  }
  }   
  
  \foreach \x in {\rl-5*\i+0.6*\a} {
  \foreach \y in {\rl-5*\i+0.6*\a} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(-\d+\xx)/(-\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:0.9*\a);
  }
 }   
  
  
  
  
  
  %number 3%  
\foreach \x in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\foreach \y in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:\a);
  }
  }    
  \foreach \x in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\foreach \y in {-\rl+5*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{0.5*(\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:\a);
 }
 }   
  
  
   \foreach \x in {-\rl+5*\i} {
\foreach \y in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{2*(\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw [thick, blue]  (\xx, \yy) -- ++(\zz:\a);
  }
  }   
  
  \foreach \x in {-\rl+5*\i-0.6*\a} {
  \foreach \y in {-\rl+5*\i-0.6*\a} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:0.9*\a);
  }
 }   
  
  %number 4%
\foreach \x in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\foreach \y in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(-\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw [thick, blue]  (\xx, \yy) -- ++(\zz:\a);
  }
  }           
  \foreach \x in {\rl,\rl-\i, \rl-2*\i, \rl-3*\i, \rl-4*\i} {
\foreach \y in {-\rl+5*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{0.5*(-\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw [thick, blue] (\xx, \yy) -- ++(\zz:\a);
 }
 }   
  
  
   \foreach \x in {\rl-5*\i} {
\foreach \y in {-\rl,-\rl+\i, -\rl+2*\i, -\rl+3*\i, -\rl+4*\i} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{2*(-\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:\a);
  }
  }   
  
  \foreach \x in {\rl-5*\i+0.6*\a} {
  \foreach \y in {-\rl+5*\i-0.6*\a} {
\pgfmathsetmacro{\xx}{\x};
\pgfmathsetmacro{\yy}{\y};
\pgfmathsetmacro{\z}{(-\d+\xx)/(\d+\yy)};
\pgfmathsetmacro{\zz}{180+atan(\z)};
 \draw[thick, blue]  (\xx, \yy) -- ++(\zz:0.9*\a);
  }
 }   
  \filldraw[yellow] (0,0) circle (\b);
%\node at (0,0) {\textbullet};  
\filldraw[black] (0,0) circle (\c);  
\draw[yellow] (\rl-\b,\rl) arc (180: 270:\b);
\fill[yellow] (\rl-\b,\rl) arc (180: 270:\b)--(\rl,\rl)--cycle;
\fill[yellow] (-\rl,\rl-\b) arc (270: 360:\b)--(-\rl,\rl)--cycle;
\draw[yellow] (-\rl+\b,-\rl) arc (0: 90:\b);
\fill[yellow] (-\rl+\b,-\rl) arc (0: 90:\b)--(-\rl,-\rl)--cycle;
\draw[yellow] (\rl,-\rl+\b) arc (90: 180:\b);
\fill[yellow] (\rl,-\rl+\b) arc (90: 180:\b)--(\rl,-\rl)--cycle;

\draw[black] (\rl-\c,\rl) arc (180: 270:\c);
\fill[black] (\rl-\c,\rl) arc (180: 270:\c)--(\rl,\rl)--cycle;
\draw[black] (-\rl,\rl-\c) arc (270: 360:\c);
\fill[black] (-\rl,\rl-\c) arc (270: 360:\c)--(-\rl,\rl)--cycle;
\draw[black] (-\rl+\c,-\rl) arc (0: 90:\c);
\fill[black] (-\rl+\c,-\rl) arc (0: 90:\c)--(-\rl,-\rl)--cycle;
\draw[black] (\rl,-\rl+\c) arc (90: 180:\c);
\fill[black] (\rl,-\rl+\c) arc (90: 180:\c)--(\rl,-\rl)--cycle;    


     \end{tikzpicture} 
   %  \end{document}

    

